<Window x:Class="CodexQuotaWidget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Codex 额度"
        Width="224"
        SizeToContent="Height"
        MinWidth="224"
        MinHeight="112"
        MaxWidth="224"
        MaxHeight="142"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="{StaticResource PanelBrush}"
        AllowsTransparency="False"
        WindowStartupLocation="Manual"
        ShowActivated="True"
        SourceInitialized="WindowSourceInitialized"
        Loaded="WindowLoaded"
        ContentRendered="WindowContentRendered"
        Closed="WindowClosed"
        MouseLeftButtonDown="WindowMouseLeftButtonDown">
    <Border Margin="0"
            ToolTip="{Binding StatusText}"
            Padding="10,8,10,7"
            Background="{StaticResource PanelBrush}"
            BorderBrush="{StaticResource PanelBorderBrush}"
            BorderThickness="1"
            CornerRadius="4">
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="刷新额度" Click="RefreshClicked" />
                <Separator />
                <MenuItem Header="退出小组件" Click="ExitClicked" />
            </ContextMenu>
        </Border.ContextMenu>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="22" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Ellipse Grid.Column="0" Width="7" Height="7" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{StaticResource AccentBrush}" />
                <TextBlock Grid.Column="1" Text="Codex" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
                <Border Grid.Column="2" Margin="6,0,0,0" Padding="5,1" VerticalAlignment="Center" Background="#333333" CornerRadius="3">
                    <TextBlock Text="{Binding PlanName}" FontSize="12" Foreground="#C8C8C8" />
                </Border>
                <Button Grid.Column="4" Width="22" Height="20" Padding="0" BorderThickness="0" Background="Transparent" Foreground="{StaticResource SecondaryTextBrush}" FontSize="14" Content="↻" ToolTip="立即刷新额度" Click="RefreshClicked" />
            </Grid>

            <Grid Grid.Row="1" Margin="0,4,0,0" Visibility="{Binding FiveHourVisibility}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="23" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="34" />
                    <ColumnDefinition Width="39" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="5h" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}" VerticalAlignment="Center" />
                <ProgressBar Grid.Column="1" Height="5" Margin="0,0,6,0" VerticalAlignment="Center" Minimum="0" Maximum="100" Value="{Binding FiveHourPercent, Mode=OneWay}" Background="#333333" Foreground="{StaticResource AccentBrush}" BorderThickness="0" />
                <TextBlock Grid.Column="2" Text="{Binding FiveHourPercentText}" FontSize="13" HorizontalAlignment="Right" VerticalAlignment="Center" />
                <TextBlock Grid.Column="3" Text="{Binding FiveHourResetText}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" />
            </Grid>

            <Grid Grid.Row="2" Visibility="{Binding SevenDayVisibility}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="23" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="34" />
                    <ColumnDefinition Width="39" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="7d" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}" VerticalAlignment="Center" />
                <ProgressBar Grid.Column="1" Height="5" Margin="0,0,6,0" VerticalAlignment="Center" Minimum="0" Maximum="100" Value="{Binding SevenDayPercent, Mode=OneWay}" Background="#333333" Foreground="{StaticResource WeeklyBrush}" BorderThickness="0" />
                <TextBlock Grid.Column="2" Text="{Binding SevenDayPercentText}" FontSize="13" HorizontalAlignment="Right" VerticalAlignment="Center" />
                <TextBlock Grid.Column="3" Text="{Binding SevenDayResetText}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" />
            </Grid>

            <Grid Grid.Row="3" Margin="0,2,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding MembershipText}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                <Border Grid.Column="1" MinWidth="20" Height="18" Padding="5,0" Background="{StaticResource SuccessBrush}" CornerRadius="8" VerticalAlignment="Center">
                    <TextBlock Text="{Binding ResetCreditsText}" FontSize="12" Foreground="#15221F" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
